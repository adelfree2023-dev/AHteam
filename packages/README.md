# 📦 Packages

## ⚠️ تحذير حاسم

```
╔═══════════════════════════════════════════════════════════════════════════╗
║                                                                           ║
║   جميع الـ Packages في هذا المجلد هي:                                      ║
║                                                                           ║
║   🏭 FACTORY-SIDE ONLY                                                    ║
║                                                                           ║
║   ❌ ليست Runtime بعد التصدير                                             ║
║   ❌ لا يُسمح لأي Template باستدعائها                                      ║
║   ❌ لا تظهر في Generated Artifact                                        ║
║                                                                           ║
╚═══════════════════════════════════════════════════════════════════════════╝
```

---

## 📋 الـ Packages الموجودة

| Package | الوظيفة | الاستخدام |
|---------|---------|-----------|
| `@ahteam/core-domain` | Domain Entities & Value Objects | Generator فقط |
| `@ahteam/tenant` | Multi-Tenant Logic | Generator فقط |
| `@ahteam/auth` | Authentication & Authorization | Generator فقط |
| `@ahteam/shared` | Utilities & Types | Generator فقط |

---

## 🚫 قواعد صارمة

### ❌ ممنوع:

```typescript
// ❌ داخل أي Template
import { Tenant } from '@ahteam/core-domain';

// ❌ داخل أي Generated Code
import { TenantContext } from '@ahteam/tenant';

// ❌ داخل أي Client-side Code
import { AuthContext } from '@ahteam/auth';
```

### ✅ مسموح:

```typescript
// ✅ داخل Generator فقط
import { Tenant } from '@ahteam/core-domain';

// ✅ داخل Factory API فقط
import { TenantContext } from '@ahteam/tenant';
```

---

## 🎯 الغرض

هذه الـ Packages تُستخدم فقط:

1. **للتحقق** - التأكد من صحة البيانات قبل التوليد
2. **للمعالجة** - تحويل البيانات للشكل المطلوب
3. **للتوليد** - إنشاء الكود النهائي

**بعد التوليد:**
- الـ Packages تختفي
- المنتج لا يعرفها
- العميل لا يحتاجها

---

> *Factory Packages = أدوات داخلية، ليست جزء من المنتج*
