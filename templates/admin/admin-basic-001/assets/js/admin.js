/**
 * ============================================
 * AHteam Admin - Main JS
 * ============================================
 */

const Admin = {
    init: function () {
        console.log('Admin Panel Initialized');
    }
};

// Initialize on load
document.addEventListener('DOMContentLoaded', Admin.init);
window.Admin = Admin;
